# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_release --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------
#
# Customisations:
# BE SURE TO RE-APPLY THE CUSTOMISATIONS IF YOU RE-GENERATE THIS FILE.
# * actions/checkout@v2 -> set to @v3
# * actions/checkout -> add fetch-depth: 0
# * Set the name of the job to 'release'
# * Add docker-login action - we can docker login in the Nuke build but can't do --password-stdin so get ugly
#   (but harmless) warnings and errors in the actions output

name: release

on:
  push:
    branches:
      - main
      - origin/main

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Run './build.cmd Cover DockerPush'
        run: ./build.cmd Cover DockerPush
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/upload-artifact@v1
        with:
          name: TestCoverage
          path: tests/TestCoverage
      - uses: actions/upload-artifact@v1
        with:
          name: TestResults
          path: tests/TestResults
